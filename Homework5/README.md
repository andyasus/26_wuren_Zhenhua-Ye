# 最优化与控制作业

## 一、编译与运行
### 作业1
**使用MATLAB运行**

### 作业2

```bash
cd Homework5/作业2
mkdir -p build && cd build
cmake ..
make
```

**Q1 — 梯度下降法**

```bash
cd Homework5/作业2/build
./q1_gradient_descent
```

**Q3 — OSQP 二次规划求解**

```bash
cd Homework5/作业2/build
export LD_LIBRARY_PATH=/tmp/osqp/build/out:$LD_LIBRARY_PATH
./q3_osqp_solve
```

## 二、学习记录
1. **听课**：听完几个数学推导，感觉有点懵懵的。但是用平时觉得比较枯燥的微积分作为工具去推导运动学的物理问题让我觉得这些问题容易理解了，还是学有所用的。
2. **作业1**：
   - 第一问运动学自行车模型:运用视频中讲的模型来做；拓展题的侧偏力公式没有学过，自己学了一下。最后成功输出了一蓝一红两个圆。
   - 第二问轨迹跟踪:运用视频中的纯追踪法来实现；进一步学习了MATLAB的语法，了解到atan和atan2的区别，并运用**atan2**把超出[-pi,pi]范围的角度转化为范围内的角度（**归一化**）；调试Ld参数时发现了一个有趣的现象：当**Ld=0.2**时红色轨迹**最贴合**参考轨迹，当Ld的值在**5~8**之间轨迹**起始稍有偏差**，后面轨迹也很贴合参考轨迹，当**Ld较大时（比如20）**时出现了视频中提到的**切弯**轨迹。询问DeepSeek后了解到Ld=0.2时车辆相当于盯着脚下的前两三个点看，虽然轨迹很贴合，但是车辆转向其实非常急促，一直在猛打方向，而当Ld>=5的时候虽然有点偏差，但真实车辆执行会更平顺。
3. **作业2**： 
   - 第一问：梯度下降，用到了这个学期的微积分知识，还比较容易理解;
   - 第二问：拉格朗日乘数法，也是这学期学的，还OK。学习率调到0.18的时候迭代结果比较好；
   - 第三问：**哈哈线性代数是什么哈哈忘了……** 好吧还是复习了一下二次型的知识。不过OSQP求解器确实不知道怎么用，问了DeepSeek来配置和教我使用，然后我把使用的语法写在了代码的注释里，便于自己理解。
